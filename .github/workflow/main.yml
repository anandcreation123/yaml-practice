name: Anand First Workflow

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome Message
        run: echo "Hello Anand"

      - name: Show Date
        run: date

      - name: List Files
        run: ls

      - name: Show Current User
        run: whoami
